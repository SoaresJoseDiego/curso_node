<h1>Produtos</h1>

<div class="products">
    {{#each products}}
    <div class="products">
        <h3>{{this.name}}</h3>
        <img src="{{this.image}}" alt="{{this.name}}">
        <p>R${{this.price}}</p>
    </div>
    {{/each}}
</div>